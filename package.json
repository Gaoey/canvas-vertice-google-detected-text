{
  "name": "google-detected-text-canvas",
  "version": "1.0.0",
  "description": "gaoey",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "browserify draw.js -o bundle.js && node -r esm app.js",
    "build": "browserify draw.js -o bundle.js"
  },
  "dependencies": {
    "esm": "^3.2.25",
    "express": "^4.17.1",
    "fast-csv": "^4.3.2",
    "ramda": "^0.27.1"
  }
}
